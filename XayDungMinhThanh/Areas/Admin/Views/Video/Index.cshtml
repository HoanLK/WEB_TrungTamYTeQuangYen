@{
    ViewBag.Title = "Quản lý Video";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div ng-controller="videoController">
    <!--Header Section-->
    <div class="section-header">
        <ol class="breadcrumb">
            <li class="active text-uppercase">Quản lý Video</li>
        </ol>
    </div>

    <!--Body-->
    <div class="section-body">
        @*<input type="hidden" value="@WebSecurity.CurrentUserName" id="user" />*@
        <div class="row">

        </div>
        <hr class="ruler-lg" />

        <div class="card">
            <div class="card-body">
                @*<span us-spinner="{radius:15, width:4, length: 8}"></span>*@
                <!--Category Post-->
                <div class="col-md-12 col-sm-12 col-xs-12" id="post">
                    <div id="gridContainer" dx-data-grid="gridVideos" dx-item-alias="video">
                        <div data-options="dxTemplate:{ name:'cellTemplateImage' }">
                            @*<img ng-src="{{video.value}}" style="height: 200px" />*@
                            @*<iframe width="560" height="315" src="{{link.value}}" allowfullscreen></iframe>*@
                            {{link.value}}
                        </div>
                    </div>
                    <div dx-context-menu="contextMenuVideo"></div>
                </div>

                <!--Popup Delete-->
                <div class="popup" dx-popup="popupDeleteVideo">
                    <div data-options="dxTemplate: { name:'templateDeleteVideo' }">
                        <div style="text-align: center">{{titleDeleteVideo}}</div>
                        <br />
                        <div dx-button="{ text: 'Có', icon: 'floppy', onClick: ConfirmDeleteVideo, type: 'success' }"></div>
                        <div dx-button="{ text: 'Không', icon: 'clear', onClick: CancelDeleteVideo, type: 'danger' }"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>