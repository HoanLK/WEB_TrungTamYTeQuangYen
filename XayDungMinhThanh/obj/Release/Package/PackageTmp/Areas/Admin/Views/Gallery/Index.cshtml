
@{
    ViewBag.Title = "Quản lý Thư viện ảnh";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div ng-controller="galleryController">
    <!--Header Section-->
    <div class="section-header">
        <ol class="breadcrumb">
            <li class="active text-uppercase">Quản lý Thư viện ảnh</li>
        </ol>
    </div>

    <!--Body-->
    <div class="section-body">
        @*<input type="hidden" value="@WebSecurity.CurrentUserName" id="user" />*@
        <div class="row">

        </div>
        <hr class="ruler-lg" />

        <div class="card">
            <div class="card-body">
                @*<span us-spinner="{radius:15, width:4, length: 8}"></span>*@
                <!--Category Gallery-->
                <div class="col-md-12 col-sm-12 col-xs-12" id="gallery">
                    <div id="gridContainer" dx-data-grid="gridGalleries"></div>
                    <div dx-context-menu="contextMenuGallery"></div>
                </div>

                <!--Popup Delete-->
                <div class="popup" dx-popup="popupDeleteGallery">
                    <div data-options="dxTemplate: { name:'templateDeleteGallery' }">
                        <div style="text-align: center" ng-cloak>{{titleDeleteGallery}}</div>
                        <br />
                        <div dx-button="{ text: 'Có', icon: 'floppy', onClick: ConfirmDeleteGallery, type: 'success' }"></div>
                        <div dx-button="{ text: 'Không', icon: 'clear', onClick: CancelDeleteGallery, type: 'danger' }"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>




